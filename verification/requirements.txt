httpx==0.27.2
pydantic==2.9.2
# Local-run convenience on TLS-inspecting networks: use the OS trust store so the
# corporate/inspecting root is trusted. Harmless on clean CI where system certs work.
truststore==0.9.2
